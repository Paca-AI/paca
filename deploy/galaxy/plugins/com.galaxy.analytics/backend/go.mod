// Stub module — intentionally dependency-free (no plugin-sdk-go: this plugin
// has no backend behaviour; see main.go for why the .wasm must exist at all).
module galaxy.local/com.galaxy.analytics/backend-stub

go 1.24
