# Base URL of the running application.
# Default: http://localhost
E2E_BASE_URL=http://localhost

# Credentials for the test user account.
# These must match a real account in your local/test environment.
# Default values are used automatically when these vars are not set.
E2E_USERNAME=admin
E2E_PASSWORD=e2e-admin-password
